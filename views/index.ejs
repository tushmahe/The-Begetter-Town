<%- include("partials/boiler") -%>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="/css/cardstyle.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Fredoka+One&family=Signika+Negative:wght@400;700&display=swap" rel="stylesheet">

  <%- include("partials/header") -%>

    <div class="cara">
      <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
        <ol class="carousel-indicators">
          <% for(var i = 0; i < upcoming.length; i++){ %>
            <% if(i == 0){ %>
          <li data-target="#carouselExampleIndicators" data-slide-to="<%= i %>" class="active"></li>
            <% } else { %>
              <li data-target="#carouselExampleIndicators" data-slide-to="<%= i %>"></li>
              <% } %>
          <% } %>
        </ol>
        <div class="carousel-inner">
          <% for(var i = 0; i < upcoming.length; i++){ %>
            <% if(i == 0) { %>
          <div class="carousel-item active">
            <img class="d-block w-100" src="\img\eventPic\<%= upcoming[i].eventPic %>" alt="First slide" height="300" width="70">
          </div>
          <% } else { %>
            <div class="carousel-item">
              <img class="d-block w-100" src="\img\eventPic\<%= upcoming[i].eventPic %>" alt="event slide" height="300" width="70">
            </div>
            <% } %>
          <% } %>
        </div>
        <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="sr-only">Next</span>
        </a>
      </div>
    </div>

    <p class="explore" style="font-family: 'Fredoka One', cursive; font-size: 40px; color: rgba(0, 0, 0, 68%); margin-bottom: 5rem;;;"><u>SOME FRESH CREATIONS FOR YOU</u></p>
    <div class="cards">
      <% for(var i = 0; i < allPosts.length; i++){ %>
        <div class="card" style="border: 10px; border-color: black;">
          <img class="card-img-top" src="img/postPic/<%= allPosts[i].postPicture %>" alt="Card image cap">
          <div class="card-body mb-4 card_content">
            <h3 class="card_title" style="margin-bottom:20px; "><%= allPosts[i].Title %></h3>
            <span class="card_subtitle" style="font-size: 15px; font-weight: bold;">Created By: <a href="/profile/<%= allPosts[i].Username %>" style="color:blue;"><%= allPosts[i].Username %></a></span>
            <p class="card-text card_description" ;;><%=allPosts[i].Description.substring(0, 90)%><br><a href="/post_details/<%= allPosts[i].Title %>">Read more</a></p>
          </div>
        </div>
      <% } %>
    </div>
    <div class="aboutUs" id="About_us">
      <h2 style="font-family: 'Fredoka One', cursive; font-size: 30px; color: rgba(35, 34, 34, 0.537);">ABOUT US</h2>
      <p class="aboutustext">
        We aim to provide local artists and creators, who have just begun their career and need a platform where they can present and showcase their own creations to the public because having individual platforms for this purpose at the very beginning stage can lead to high investment. The main motivation is to create common platform where creators can post their own creations, the public can view the creations and contact the creators according to their own interest and awareness about upcoming/past events is available can really be helpful. We also hope to add a payment section to the website so as to promote e-commerce between users and creators.
      </p>
    </div>

    <%- include("partials/footer") -%>
      </body>